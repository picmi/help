export function parse(): never {
    throw new Error('dpack persistence is not available in the browser build');
}

export function serialize(): never {
    throw new Error('dpack persistence is not available in the browser build');
}
